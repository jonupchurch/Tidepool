# Changelog

Dated dev-progress log, newest first. Will adopt versioned release notes once Steam builds start.

## 2026-07-24

- `git init` on `main`; bootstrapped Spec-Kit into the repo from the ai-tools toolkit (`.claude/`, `.specify/`, constitution, `docs/`, `stacks/README.md`) — omitted the Next.js stack pack.
- Added `.gitignore` tuned for Vite/TS/Tauri (commits `specs/`; ignores `.specify/feature.json`, build/test cruft).
- Tuned the constitution → **v2.1.0**: strengthened Principle I (ask over assume), added TL;DR brevity to Principle VI, added Principle X (maintain `PLAN.md` / `STATUS.md` / `CHANGELOG.md`).
- Created `PLAN.md`, `STATUS.md`, `CHANGELOG.md`.
