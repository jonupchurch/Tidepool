# Tidepools — Status

_Snapshot; updated each work session. Last updated: 2026-07-24._

## Current phase

Project setup — Spec-Kit bootstrapped and constitution tuned. Not yet writing feature specs or code.

## Done

- Reviewed `resources/` design package (brief, style guide, 11 screen mockups).
- Chose stack: Vite + TS + Canvas + Tauri for Steam (see `PLAN.md`).
- `git init`; bootstrapped the ai-tools Spec-Kit toolkit into the repo (minus the Next.js stack pack).
- Tuned constitution → v2.1.0 (ask-over-assume, TL;DR brevity, Principle X: living artifacts).
- Created `PLAN.md`, `STATUS.md`, `CHANGELOG.md`.

## Next

- Write `stacks/tidepools.md` stack pack.
- Decide whether to add the seed-determinism invariant as a constitution principle.
- Start the Spec-Kit chain: `/speckit-specify` → `/speckit-plan` across the feature set (engine first).
- Land an initial commit once constitution edits are approved.

## Blockers

- None.
