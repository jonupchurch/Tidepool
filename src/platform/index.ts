// platform — the seam between the game and the OS: save/load, settings,
// achievements, file paths. Web (localStorage/IndexedDB) vs Tauri impls live
// here; nothing OS-specific leaks into core/game/ui.
export {}
