// AppShell.tsx — the app root: hosts navigation state, swaps the active screen,
// applies the persisted theme, and overlays Pause. Implemented in Phase 2 (T009).
export function AppShell() {
  return null
}
