// ResumeCard.tsx — Home's "Continue your pool" affordance. Implemented in US2.
import type { ResumeSnapshot } from './types'

export interface ResumeCardProps {
  snapshot: ResumeSnapshot
  onResume: () => void
}

export function ResumeCard(_props: ResumeCardProps) {
  return null
}
